import React from "react";
import Header from "../components/Header";
import styled from "styled-components";

const Pokedex = (props) => {
  return <Header title="POKEDEX" name="Voltar para lista de pokemons" />;
};

export default Pokedex;
